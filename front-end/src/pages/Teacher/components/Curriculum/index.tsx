import React from "react";
import styles from "./Curriculum.module.scss";
import EpisodeForm from "./EpisodeForm";

const Curriculum = () => {
  return <div className={styles.container}>Curriculum
    <EpisodeForm />
  </div>;
};

export default Curriculum;
